# Example environment variables for ISD-v1.0
# Copy to .env and update values before running

# Flask
APP_SECRET_KEY=change-me-to-a-strong-random-string
FLASK_ENV=development

# Database (SQLite by default)
# For SQLite (default), you can use:
DATABASE_URL=sqlite:///./app.db

# Or for MySQL (example):
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=secret
DB_NAME=isd

# CORS / Frontend origin (if any)
FRONTEND_ORIGIN=http://localhost:3000

# Optional: Tesseract path (Windows)
# TESSERACT_CMD=C:\\Program Files\\Tesseract-OCR\\tesseract.exe

# Other optional settings
# LOG_LEVEL=INFO
